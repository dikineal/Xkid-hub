-- =================================================================
-- @XKID SCANNER & REMOTE EVENT SPY V1.0
-- by @WTF.XKID | Full Auto Inspector & Hook Tool
-- =================================================================

repeat task.wait() until game:IsLoaded()

-- ================================ LOAD WINDUI ================================
local WindUI = (function()
    local s, r = pcall(function() 
        return loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))() 
    end)
    if s then return r else error("Gagal memuat WindUI") end
end)()

-- ================================ SERVICES ================================
local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LP = Players.LocalPlayer

local Inspector = {
    SpyActive = true,
    CapturedRemotes = {},
    DetectedAnimals = {},
    DetectedUI = {},
    Logs = {}
}

local function notify(title, content, duration, icon)
    pcall(function() WindUI:Notify({ Title = title, Content = content, Duration = duration or 2, Icon = icon or "bell" }) end)
end

function Inspector:Log(msg)
    local t = os.date("%H:%M:%S")
    local entry = string.format("[%s] %s", t, msg)
    table.insert(self.Logs, entry)
    if #self.Logs > 150 then table.remove(self.Logs, 1) end
end

-- ================================ REMOTE EVENT SPY ENGINE ================================
local function initRemoteSpy()
    local rawMeta = getrawmetatable(game)
    local oldNamecall = rawMeta.__namecall
    setreadonly(rawMeta, false)

    rawMeta.__namecall = newcclosure(function(self, ...)
        local method = getnamecallmethod()
        local args = {...}

        if Inspector.SpyActive and (method == "FireServer" or method == "fireServer" or method == "InvokeServer" or method == "invokeServer") then
            local remoteName = self.Name
            local remotePath = self:GetFullName()
            
            -- Format Argumen
            local argFormatted = ""
            for i, v in ipairs(args) do
                argFormatted = argFormatted .. tostring(v) .. (i < #args and ", " or "")
            end

            local spyText = string.format("%s:%s(%s)", remoteName, method, argFormatted)
            Inspector:Log("🔥 REMOTE: " .. spyText)

            table.insert(Inspector.CapturedRemotes, {
                Name = remoteName,
                Path = remotePath,
                Method = method,
                Args = args,
                FullCode = string.format("game.%s:%s(%s)", remotePath, method, argFormatted)
            })
        end

        return oldNamecall(self, ...)
    end)

    setreadonly(rawMeta, true)
    Inspector:Log("Remote Spy Hook Berhasil Dijalankan!")
end

pcall(initRemoteSpy)

-- ================================ ANIMAL & MAP SCANNER ================================
function Inspector:ScanMapAnimals()
    self.DetectedAnimals = {}
    local count = 0

    for _, obj in ipairs(Workspace:GetDescendants()) do
        if obj:IsA("Model") and obj ~= LP.Character then
            local hum = obj:FindFirstChildOfClass("Humanoid")
            local hrp = obj:FindFirstChild("HumanoidRootPart") or obj.PrimaryPart or obj:FindFirstChild("Head")

            if hum and hrp and not Players:GetPlayerFromCharacter(obj) then
                count = count + 1
                table.insert(self.DetectedAnimals, {
                    Name = obj.Name,
                    Model = obj,
                    Position = hrp.Position,
                    Path = obj:GetFullName()
                })
            end
        end
    end

    self:Log(string.format("Scan Workspace: Ditemukan %d hewan/NPC.", count))
    return count
end

-- ================================ MINIGAME UI SCANNER ================================
function Inspector:ScanMinigameUI()
    self.DetectedUI = {}
    local pGui = LP:FindFirstChildOfClass("PlayerGui")
    if not pGui then return end

    for _, gui in ipairs(pGui:GetChildren()) do
        if gui:IsA("ScreenGui") and gui.Enabled then
            for _, btn in ipairs(gui:GetDescendants()) do
                if (btn:IsA("ImageButton") or btn:IsA("TextButton") or btn:IsA("ImageLabel")) and btn.Visible then
                    table.insert(self.DetectedUI, {
                        GuiName = gui.Name,
                        Name = btn.Name,
                        Class = btn.ClassName,
                        Path = btn:GetFullName(),
                        Pos = btn.AbsolutePosition
                    })
                end
            end
        end
    end

    self:Log(string.format("UI Scan: Ditemukan %d elemen UI di layar.", #self.DetectedUI))
end

-- ================================ UI WINDUI BUILD ================================
local Window = WindUI:CreateWindow({
    Title = "XKID Remote & Animal Inspector",
    Icon = "search",
    Author = "@WTF.XKID",
    Folder = "XKID_Inspector",
    Size = UDim2.fromOffset(590, 430),
    Transparent = true,
    Theme = "Crimson",
    SideBarWidth = 170,
})

-- TAB 1: CONSOLE & REMOTE SPY
local TabSpy = Window:Tab({ Title = "Remote Spy", Icon = "terminal" })
local secSpy = TabSpy:Section({ Title = "Live Remote Console", Icon = "code", Box = true })

local logParagraph = secSpy:Paragraph({ Title = "Console Output", Desc = "Menunggu aksi tangkap hewan..." })

task.spawn(function()
    while task.wait(0.5) do
        if #Inspector.Logs > 0 then
            local text = table.concat(Inspector.Logs, "\n")
            if #text > 2200 then text = text:sub(-2200) end
            logParagraph:SetDesc(text)
        end
    end
end)

secSpy:Toggle({
    Title = "Enable Remote Spy",
    Default = true,
    Callback = function(v)
        Inspector.SpyActive = v
        notify("Remote Spy", v and "AKTIF" or "OFF", 1.5, "radio")
    end
})

secSpy:Button({
    Title = "Copy Captured Remote Code 📋",
    Desc = "Salin kode RemoteEvent ke clipboard",
    Callback = function()
        if #Inspector.CapturedRemotes > 0 then
            local str = "-- CAPTURED REMOTE EVENTS --\n"
            for i, r in ipairs(Inspector.CapturedRemotes) do
                str = str .. string.format("-- [%d] %s\n%s\n\n", i, r.Name, r.FullCode)
            end
            setclipboard(str)
            notify("Inspector", "Kode Remote berhasil di-copy!", 2, "copy")
        else
            notify("Error", "Belum ada Remote terdeteksi! Tangkap 1 hewan dulu.", 2, "alert-circle")
        end
    end
})

-- TAB 2: ANIMAL & UI SCANNER
local TabScan = Window:Tab({ Title = "Scanner Data", Icon = "scan" })
local secScan = TabScan:Section({ Title = "Map & UI Data", Icon = "database", Box = true })

secScan:Button({
    Title = "Scan Jumlah Hewan di Map 🐕",
    Callback = function()
        local total = Inspector:ScanMapAnimals()
        notify("Animal Scan", string.format("Total Hewan Spawn: %d", total), 2, "check")
    end
})

secScan:Button({
    Title = "Copy Animal Data Code 📋",
    Callback = function()
        if #Inspector.DetectedAnimals > 0 then
            local str = "-- DETECTED ANIMALS DATA --\nlocal Animals = {\n"
            for i, a in ipairs(Inspector.DetectedAnimals) do
                str = str .. string.format("    [%d] = { Name = %q, Path = %q, Pos = Vector3.new(%.1f, %.1f, %.1f) },\n", i, a.Name, a.Path, a.Position.X, a.Position.Y, a.Position.Z)
            end
            str = str .. "}\nreturn Animals"
            setclipboard(str)
            notify("Inspector", "Data hewan berhasil di-copy!", 2, "copy")
        else
            notify("Error", "Klik 'Scan Jumlah Hewan' dulu!", 2, "alert-circle")
        end
    end
})

secScan:Button({
    Title = "Scan UI Minigame (Saat Popup Aktif) 🎮",
    Callback = function()
        Inspector:ScanMinigameUI()
        if #Inspector.DetectedUI > 0 then
            local str = "-- MINIGAME UI DATA --\nlocal UIElements = {\n"
            for i, u in ipairs(Inspector.DetectedUI) do
                str = str .. string.format("    [%d] = { Gui = %q, Name = %q, Class = %q, Path = %q },\n", i, u.GuiName, u.Name, u.Class, u.Path)
            end
            str = str .. "}\nreturn UIElements"
            setclipboard(str)
            notify("Inspector", "Data UI Minigame berhasil di-copy!", 2, "copy")
        else
            notify("Error", "Buka minigame capung dulu di layar!", 2, "alert-circle")
        end
    end
})

notify("System", "XKID Inspector Ready!", 3, "rocket")
