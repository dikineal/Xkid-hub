-- =================================================================
-- @XKID ANIMAL CATCHER & MERCHANT V3.39 (FIXED MINIGAME CLICK)
-- by @WTF.XKID | Full Animal Support (Kupu-Kupu, Capung, DLL)
-- =================================================================

repeat task.wait() until game:IsLoaded()

-- ================================ WINDUI LOADER ================================
local WindUI = (function()
    local s, r = pcall(function() 
        return loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))() 
    end)
    if s then return r else error("Failed to load WindUI") end
end)()

-- ================================ SERVICES ================================
local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local VirtualInputManager = game:GetService("VirtualInputManager")
local LP = Players.LocalPlayer

-- ================================ NOTIFY HELPER ================================
local function notify(title, content, duration, icon)
    pcall(function() 
        WindUI:Notify({ Title = title, Content = content, Duration = duration or 2, Icon = icon or "bell" }) 
    end)
end

local function getLocalRoot()
    local char = LP.Character
    return char and (char:FindFirstChild("HumanoidRootPart") or char.PrimaryPart or char:FindFirstChild("Head"))
end

-- ================================ OPTIMIZED CATCH ENGINE ================================
local AutoCatchEngine = {
    Enabled = false,
    Interval = 0.05, -- Respon super cepat untuk memburu gambar bergerak
    RunningThread = nil
}

local function emulateTap(x, y)
    VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 0)
    task.wait(0.005)
    VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 0)
end

function AutoCatchEngine:Start()
    if self.RunningThread then self.RunningThread = nil end

    self.RunningThread = task.spawn(function()
        while self.Enabled do
            local playerGui = LP:FindFirstChildOfClass("PlayerGui")
            if playerGui then
                for _, gui in ipairs(playerGui:GetChildren()) do
                    if gui:IsA("ScreenGui") and gui.Enabled then
                        for _, element in ipairs(gui:GetDescendants()) do
                            if (element:IsA("ImageButton") or element:IsA("TextButton") or element:IsA("ImageLabel")) and element.Visible then
                                
                                local nameLower = element.Name:lower()
                                local parentName = element.Parent and element.Parent.Name:lower() or ""
                                local isTarget = false

                                -- 1. Deteksi Kata Kunci Hewan & Minigame
                                if nameLower:find("net") or nameLower:find("jaring") or nameLower:find("catch") 
                                   or nameLower:find("action") or nameLower:find("capung") or nameLower:find("kupu") 
                                   or nameLower:find("animal") or nameLower:find("target") or parentName:find("tangkap") 
                                   or parentName:find("minigame") then
                                    isTarget = true
                                end

                                -- 2. Deteksi Elemen Gambar di Dalam Pop-up Minigame
                                if not isTarget and (element:IsA("ImageButton") or element:IsA("ImageLabel")) then
                                    local rootGui = element:FindFirstAncestorOfClass("ScreenGui")
                                    if rootGui then
                                        for _, child in ipairs(rootGui:GetDescendants()) do
                                            if child:IsA("TextLabel") and (child.Text:lower():find("tangkap") or child.Text:lower():find("kupu") or child.Text:lower():find("capung")) then
                                                isTarget = true
                                                break
                                            end
                                        end
                                    end
                                end

                                -- 3. Fallback Tombol Jaring Hijau Bawah Layar
                                if not isTarget and element:IsA("ImageButton") and element.Image ~= "" then
                                    local pos = element.AbsolutePosition
                                    local vp = workspace.CurrentCamera.ViewportSize
                                    if pos.Y > (vp.Y * 0.55) and math.abs(pos.X - (vp.X * 0.5)) < (vp.X * 0.3) then
                                        isTarget = true
                                    end
                                end

                                -- Eksekusi Klik Presisi & Direct Event Signal
                                if isTarget then
                                    -- A. Panggil Langsung Event Internal Roblox
                                    pcall(function()
                                        for _, conn in ipairs(getconnections(element.MouseButton1Click)) do conn:Fire() end
                                        for _, conn in ipairs(getconnections(element.Activated)) do conn:Fire() end
                                        for _, conn in ipairs(getconnections(element.TouchTap)) do conn:Fire() end
                                        for _, conn in ipairs(getconnections(element.InputBegan)) do 
                                            conn:Fire({ UserInputType = Enum.UserInputType.MouseButton1, UserInputState = Enum.UserInputState.Begin }) 
                                        end
                                    end)

                                    -- B. Emulasi Ketukan Fisik di Tengah Gambar
                                    local pos = element.AbsolutePosition
                                    local size = element.AbsoluteSize
                                    if size.X > 5 and size.Y > 5 then
                                        emulateTap(pos.X + (size.X / 2), pos.Y + (size.Y / 2) + 36)
                                    end
                                end
                            end
                        end
                    end
                end
            end
            task.wait(self.Interval)
        end
    end)
end

function AutoCatchEngine:Stop()
    self.Enabled = false
    self.RunningThread = nil
end

-- ================================ MERCHANT TP ENGINE ================================
local function teleportToMerchant()
    local root = getLocalRoot()
    if not root then
        notify("Error", "Character tidak ditemukan!", 2, "alert-circle")
        return
    end

    local sellBooth = Workspace:FindFirstChild("SellBooth", true)
    if sellBooth then
        local targetCF = sellBooth:IsA("Model") and (sellBooth.PrimaryPart or sellBooth:FindFirstChildWhichIsA("BasePart")).CFrame or sellBooth.CFrame
        root.CFrame = targetCF * CFrame.new(0, 3, 3)
    else
        root.CFrame = CFrame.new(642.5, -192.0, -980.0)
    end
    root.AssemblyLinearVelocity = Vector3.zero
    notify("Merchant TP", "Berhasil Teleport ke Penjual!", 2, "store")
end

-- ================================ UI INITIALIZATION ================================
local Window = WindUI:CreateWindow({
    Title = "XKID Catcher V3.39",
    Icon = "bug",
    Author = "@WTF.XKID",
    Folder = "XKID_Catcher",
    Size = UDim2.fromOffset(560, 380),
    Transparent = true,
    Theme = "Crimson",
    SideBarWidth = 160,
})

-- ================================ TAB: CATCHER & MERCHANT ================================
local TabMain = Window:Tab({ Title = "Auto Catch & Merchant", Icon = "net" })

local secCatch = TabMain:Section({ Title = "Auto Catching System (Kupu & Capung)", Icon = "zap", Box = true })

secCatch:Toggle({
    Title = "Auto Catch / Press Net Button 🕸",
    Desc = "Auto-click presisi untuk Kupu-Kupu, Capung, dan hewan lainnya",
    Default = false,
    Callback = function(v)
        AutoCatchEngine.Enabled = v
        if v then
            AutoCatchEngine:Start()
            notify("Auto Catch", "Catcher Engine V3.39 AKTIF", 2, "check")
        else
            AutoCatchEngine:Stop()
            notify("Auto Catch", "Catcher Engine OFF", 1.5, "pause")
        end
    end
})

secCatch:Paragraph({
    Title = "Pembaruan V3.39",
    Desc = "• Menambahkan deteksi 'Kupu-Kupu' & Minigame Pop-up\n• Menggunakan Direct Signal Fire agar klik tidak meleset saat hewan bergerak\n• Mendukung semua jenis minigame hewan di map"
})

local secMerchant = TabMain:Section({ Title = "Penjual / Merchant TP", Icon = "store", Box = true })

secMerchant:Button({
    Title = "Teleport ke Penjual (SellBooth) 🏪",
    Desc = "Langsung teleport ke tempat jual hewan",
    Callback = function()
        teleportToMerchant()
    end
})

-- ================================ INIT ================================
notify("System", "XKID Catcher V3.39 Fixed Ready!", 3, "rocket")
